name: Test Self-hosted Runner

on: [push]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Say Hello
      run: echo "Hola desde el runner autoalojado 🎉"
